#!/usr/bin/env bash

selected=$(kubectl config get-contexts | fzf --header-lines=1)

if [[ -z $selected ]]; then
        exit 0
fi

selectedArray=($selected)

context=${selectedArray[0]}

kubectl config use-context $context
