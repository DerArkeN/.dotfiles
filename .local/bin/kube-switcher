#!/usr/bin/env bash

selected=$(kubectl config get-contexts | fzf --tac)

if [[ -z $selected ]]; then
        exit 0
fi

selectedArray=($selected)

context=${selectedArray[0]}

kubectl config use-context $context
